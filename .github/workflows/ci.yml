name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate markdown links (optional)
        run: echo "Docs-only repo — full CI after Phase B scaffolding"

      # Nach Phase B aktivieren:
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      # - run: npm ci
      # - run: npm run lint
      # - run: npm run typecheck
      # - run: npm run build
      # - run: npm run validate:content
